<% if @favourite.id.nil?  %>
  <%= link_to "Add to favourites",[workplace,favourite], class: "btn btn-in", :method => :post, remote: true %>
  <% else %>
<%= link_to "Remove from favourites",favourite, class: "btn btn-out" , :method => :delete, remote: true %>
<% end %>
