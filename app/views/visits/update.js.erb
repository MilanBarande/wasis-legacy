// Here you will write *javascript* that would be executed by the browser

<% if @visit.errors.any? %>
  // Render new review form with errors
  $('#checkin').html("<%= j render 'visits/checkin', visit: @visit, workplace: @workplace %>");
<% else %>
  //$(".modal-backdrop").remove();

  $('#checkin').html("<%= j render 'visits/checkin', visit: @visit, workplace: @workplace %>");
  $('#checkin_navbar').html("");
  $('.feature-rating').rating({
      size:'xs',
      step: 1,
      clearCaption: '',
      starCaptions: {1: 'Terrible', 2: 'bad', 3: 'Ok', 4: 'Good', 5: 'Very Good'},
      starCaptionClasses: {1: 'text-danger', 2: 'text-warning', 3: 'text-info', 4: 'text-primary', 5: 'text-success'}
  });
  $('#present').html("<%= j render 'visits/present', visits: @visits %>");
  $('#reviews').modal("show");
<% end %>
