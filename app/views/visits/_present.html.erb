<% Visit.where(:checkin => true).each do |visit| %>
  <% user = visit.user %>
  <% avatar_url = user.facebook_picture_url || "http://placehold.it/30x30" %>
  <%= image_tag avatar_url, class: "avatar" %>
  <p><%= user.first_name %></p>
  <p><%# visit.status unless visit.status.nil? %></p>
<% end %>
